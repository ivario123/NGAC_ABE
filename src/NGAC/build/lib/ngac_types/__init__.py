"""
NGAC types
---

This module defines the types needed to run NGAC in a meaningful manner.

These types are as follows
- NgacObject ( A type level ngac type, don't use )
- Attribute ( A type level attribute, don't use )
- UserAttribute ( Attributes used for users )
- ObjectAttribute ( Attributes used for objects )
- Policy ( A policy abstraction )
- Resource ( A resource abstraction, such as files )
- User ( A user abstraction )
"""
